# Devlog 007 — Fase 1: pipeline Tiled + terreno Kenney (rama A, 2026-09-07)

## Cambios
- `public/assets/terrain-sheet.png` (13,5 KB): 10 tiles en rejilla 132×66
  recortados del diamante superior Kenney (`Isometric Landscape`, CC0) +
  3 aguas con olas en distinta posición + derivados por tinte (arena, bosque,
  montaña, hierbas B/C). Script: `Temp/opencode/tools/derivados.mjs` (sharp).
- `scripts/gen-map.mjs` → `public/assets/maps/isla-01.json`: mapa Tiled 1.10
  isométrico 28×28, tileset embebido, capa `Suelo` (CSV de gids) + capa de
  objetos `Logica` (4 edificios + 3 spawns con propiedades). Determinista.
- `src/game/maps/island.ts`: misma lógica en TS (hash, terrainAt, tileToPx,
  LOGIC_OBJECTS). `tests/unit/tilemap.test.ts` (3 casos) regenera y compara:
  el JSON commiteado no puede desincronizarse del generador.
- `GameScene`: `preload` carga JSON+sheet; capa `Suelo` (culling automático,
  1 draw-call); decoración y zonas de oficios vía `terrainAt` importado;
  colocación inicial desde la capa `Logica` (el diseñador ya manda);
  hover con marcador diamante + click por `worldToTileXY`; agua animada con
  `putTileAt` (gid 6↔7 en celdas alternas); cámara/minimapa reescaladas al
  mundo 132×66. Sprites actuales reescalados ×1.7–1.8 hasta Fase 2A.
- `BootScene`: eliminados tiles procedurales viejos (suelo ahora es Tiled).
- `docs/ATRIBUCION-A.md`: Kenney CC0 registrado.

## Verificación
- `npm test`: 11/11. `npm run build`: OK. Smoke: `/assets/maps/isla-01.json`
  200 (3649 B) + `/assets/terrain-sheet.png` 200 (13572 B).
- Bugs cazados: faltaba `width` en el JSON; `o.x/o.y` opcionales en tipos Tiled.

## Checkpoint
- Tag: `proto-a-fase1`. Porte a B: cherry-pick (mismo terreno CC0, válido en GPL).
